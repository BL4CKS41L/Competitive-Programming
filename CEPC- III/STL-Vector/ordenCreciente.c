#include <stdio.h>

int main(){
    int T=0,i;
    scanf("%d",&T);
    int arr[T];
    int  a=0;
    
    for( i = 0; i < T; i++)  {
        scanf("%d",&arr[i]);
    }

    
    for( i = 0; i < T; i++) {
        if (arr[i] <= arr[i+1]); 
        
    }
    


return 0;
}